#: import ScreenHeader components.widgets.screen_header

<SetupButton@Button>:
  font_size: 22

<SetupScreen>:
  BoxLayout:
    title: "Setup"
    orientation: "vertical"
    padding: 10
    ScreenHeader:
      text: "Setup"

    GridLayout:
      spacing: 36
      padding: 36
      cols: 4
      SetupButton:
        text: "Scale 0: " + app.scales[0].axisName
        on_release: app.manager.goto("scale_0")
      SetupButton:
        text: "Scale 1: " + app.scales[1].axisName
        on_release: app.manager.goto("scale_1")
      SetupButton:
        text: "Scale 2: " + app.scales[2].axisName
        on_release: app.manager.goto("scale_2")
      SetupButton:
        text: "Scale 3: " + app.scales[3].axisName
        on_release: app.manager.goto("scale_3")
      SetupButton:
        text: "Servo"
        on_release: app.manager.goto("servo")
      SetupButton:
        text: "Network"
        on_release: app.manager.goto("network")
      SetupButton:
        text: "Formats"
        on_release: app.manager.goto("formats")
      SetupButton:
        text: "Update"
        on_release: app.manager.goto("update")
