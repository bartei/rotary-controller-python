<ScreenHeader>:
  height: 60
  size_hint_y: None
  text: "Placeholder"
  halign: "center"
  valign: "center"

  BoxLayout:
    orientation: "vertical"
    BoxLayout:
      orientation: "horizontal"
      Label:
        text: root.text
        text_size: root.size
        bold: True
        font_size: app.formats.font_size
        valign: root.valign
        halign: root.halign
      Button:
        size_hint_x: None
        width: 250
        text: "Back"
        font_size: app.formats.font_size
        on_press: app.beep()
        on_release:
          app.manager.back()
    Widget:
      height: 3
      width: root.width
      size_hint: None, None
      canvas:
        Color:
          rgba: 0.25, 0.25, 0.75, 1
        Rectangle:
          size: self.size
          pos: self.pos
